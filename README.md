# kvn-flask-api

Simple Flask API used as a DevOps / SRE learning project.

## Endpoints

- `/`  
  Returns a small HTML greeting from KVN DevOps Flask API.

- `/health`  
  Returns JSON health status:

  ```json
  { "status": "ok" }
